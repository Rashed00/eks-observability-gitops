# Databases folder dashboards

Empty for the demo. Add `GrafanaDashboard` CRs here (one file per dashboard)
once you have a database stack to monitor (PostgreSQL exporter, MySQL,
MongoDB, Redis, ...).

See AAU reference `/workspace/aau/repositories/infra-gitops/environments/eks-observability/kustomize/grafana/dashboards/databases/`
for examples (mongodb-overview, dragonfly, mysql-8-overview).
